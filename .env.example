# 复制为 .env.local 并填入 Sentry 项目 DSN（Settings → Client Keys）
VITE_SENTRY_DSN=

# 可选：自建埋点接收地址（POST JSON），不配则仅开发环境 console 打印
# VITE_ANALYTICS_ENDPOINT=https://your-api.com/analytics/collect

# 可选：线上直连 API 前缀（需后端开 CORS）。不配则走相对路径 /api，由 vercel.json 转发到后端
# VITE_API_BASE=https://你的域名或网关/api
